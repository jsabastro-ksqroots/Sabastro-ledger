/** Shared between the edge middleware and the server (no Node-only imports here). */
export const SESSION_COOKIE_NAME = "sl_session";
export const ENTITY_COOKIE_NAME = "sl_entity";
export const YEAR_COOKIE_NAME = "sl_year";
